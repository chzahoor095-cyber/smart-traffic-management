#ifndef VEHICLE_DETECTION_H\n#define VEHICLE_DETECTION_H\n\n#include <vector>\n#include <string>\n#include <map>\n\nstruct DetectedVehicle {\n    int id;\n    std::string vehicle_type;\n    double confidence;\n    double x_position;\n    double y_position;\n    double speed;\n    std::string direction;\n    long long detection_time;\n};\n\nclass VehicleDetection {\npublic:\n    VehicleDetection();\n    ~VehicleDetection();\n    \n    void initialize_cameras(const std::vector<std::string>& camera_sources);\n    void process_frame(int camera_id, const std::string& frame_data);\n    std::vector<DetectedVehicle> get_detected_vehicles(int camera_id);\n    int count_vehicles(int camera_id);\n    void set_confidence_threshold(double threshold);\n    \nprivate:\n    std::vector<std::string> camera_sources;\n    std::map<int, std::vector<DetectedVehicle>> detected_vehicles;\n    double confidence_threshold;\n};\n\n#endif // VEHICLE_DETECTION_H